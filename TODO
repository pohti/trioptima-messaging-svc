--- done ---
- define models
    - message
- setup REST api endpoints using fastapi
- openapi specs
    - ensure the descriptions are clear
- integrate with a lightweight db (sqlite)
- unit tests
    - generate test coverage report
- dockerfile and docker compose
- README
- makefile

--- good to have ---
- make db str configurable via env vars
- validate whether recipient/sender emails follow the email format
- rabbitMQ to handle messaging queue and delete messages queue
- split up messaging service into ingest and read services
- allow soft delete (archiving) of messages
- redis or in-memory caching for reads
