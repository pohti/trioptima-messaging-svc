events {
    worker_connections 1024;
}

http {
    # Delete service upstream with load balancing
    upstream delete_backend {
        # Round-robin load balancing (default)
        server messaging-delete:8003;
        # Docker will automatically distribute to replicas
    }
    
    server {
        listen 80;
        server_name localhost;
        
        # Health check endpoint
        location /health {
            return 200 "Delete Load Balancer OK";
            add_header Content-Type text/plain;
        }
        
        # Proxy all requests to delete backend
        location / {
            proxy_pass http://delete_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Add load balancer info for debugging
            add_header X-Load-Balancer "delete-lb";
        }
    }
}